class Category {
  constructor (id, userId, name, type, sum, limit) {
    this.id = id
    this.userId = userId
    this.name = name
    this.type = type
    this.sum = sum
    this.limit = limit
  }
}

export default Category
